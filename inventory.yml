all:
  hosts:
    vps-ia:
      ansible_host: "{{ lookup('env', 'PUBLIC_IP') }}"
      # Use root for the first deployment. Set ANSIBLE_USER=deploy afterwards.
      ansible_user: "{{ lookup('env', 'ANSIBLE_USER') | default('root', true) }}"
      ansible_python_interpreter: /usr/bin/python3